import React from 'react';
import { Link } from 'react-router-dom';

const Tasks = () => (
    <div>
        <h2>Tasks</h2>
        <p>Here, you'll see a list of tasks saved in local storage.</p>
    </div>
);

export default Tasks;